# @fastbackend/core

## 0.1.2

### Patch Changes

- Add Express runtime adapter with Prisma persistence, CLI project env loading, dev port resolution, Prisma optional-field parser fix, and root health routes for Express and FastAPI runtimes.

## 0.1.1

### Patch Changes

- Initial public release of the FastBackend schema-driven backend runtime.

  - `@fastbackend/core`: IR generation, SQLAlchemy and Prisma parsers, OpenAPI output
  - `@fastbackend/cli`: `init`, `generate`, `dev`, `build`, `test`, `migrate`, and `docker:build` commands
