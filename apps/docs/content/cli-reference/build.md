---
title: build
description: Build production IR and OpenAPI artifacts.
---

# fastbackend build

Generate production IR and OpenAPI (same as `generate` for the MVP).

```bash
fastbackend build
```

Use in CI before deploying. Ensure `.fastbackend/ir.json` is available to the runtime in your deployment environment.
